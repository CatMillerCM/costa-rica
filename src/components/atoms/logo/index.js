import styles from './logo.module.css';

const Logo = ({  }) => {
  return (
    <div>
      <p className={styles.costa}>Costa</p>
      <p className={styles.rica}>Eu-rica!</p>
    </div>
  )
};

export { Logo };
